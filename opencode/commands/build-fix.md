---
agent: build-error-resolver
subtask: true
---

Resolve build and compilation errors with minimal, surgical changes.

Run the project's build command, categorize all errors, and apply the smallest fix for each.
No refactoring, no feature additions — only make the build pass. Re-verify after each fix.

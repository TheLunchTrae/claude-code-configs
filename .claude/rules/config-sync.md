# Config Sync
This repo maintains parallel Claude Code and OpenCode configurations. When any config file is created or modified — `rules/`, `settings.json`, `agents/`, `skills/` — the corresponding OpenCode equivalent in `opencode/` must be updated in the same commit. See `.claude/skills/opencode-sync/SKILL.md` for the full mapping and translation rules.
